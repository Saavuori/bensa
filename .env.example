# Copy to .env for local development. The production stack sets these itself
# (see deploy/docker-compose.yml) — no secrets are required to run bensa, it is
# built entirely on open Statistics Finland data.
REDIS_URL=redis://localhost:6379
PORT=8081
